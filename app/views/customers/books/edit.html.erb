<h4>投稿編集画面</h4>
<%= form_with model:@book, url: customers_book_path{@book.id}, local:true do |f| %>
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <h4>タイトル</h4>
  <%= f.text_field :name %>
  <h4>説明</h4>
  <%= f.text_area :body %>
  <%= f.submit '保存' %>
<% end %>